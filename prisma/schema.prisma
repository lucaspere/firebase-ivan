// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:/home/lucas/projetos/Note_App/dev.db"
}

model Note {
  id          String @id() @default(uuid())
  title       String
  description String
}
